# declaring the list
browsing_session = []
# adding the elements in the list
browsing_session.append(1)
browsing_session.append(2)
# popping the elements
browsing_session.pop()
# if there's no element left after popping then print the list
if not browsing_session:
    browsing_session[-1]
print(browsing_session)

# but still a element is remained in the list if the list would
# be empty i would just print the empty list
