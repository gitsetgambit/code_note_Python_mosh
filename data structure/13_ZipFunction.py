group1 = [1, 3, 2]
group2 = [10, 20, 30]

print(list(zip("aditya", group1, group2)))
# zip is built in funtion to bind two or more list in tupples
# it can also take "string" here it string is bigger then the
# numbers in list it'll simply ignore it
